# Sample .gitignore for testing

# Files to backup (marked with # safe on the line above)
# safe
.env
# safe
config/secrets.yml
# safe
*.key

# Files ignored but NOT backed up (no tag)
node_modules/
*.log
.cache/

# Edge cases
# safe
.credentials
# safe
some/deep/path/secret.json

# Comment line with safe in it (should NOT match)
# this is safe but just a comment

# Empty pattern (should be ignored)
# safe
